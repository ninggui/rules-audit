---
name: user-rules-audit
description: 规则审计：检查用户近7天定的规则是否已固化到 memory/skill，缺失自动补。触发：用户说"你又忘了"或夜间迭代。
slug: rules-audit
displayName: 规则审计
version: 1.0.0
---

# 用户规则审计（user-rules-audit）

## 背景

2026-08-18 用户观察：规则 1-2 天执行好、3-4 天衰减、一周忘。根因：memory 容量有限（~2200字符）+ 对话规则不落文件 + 无定期审计。

## 触发条件

- cron 每周一 02:30 自动执行（user-rules-audit，`30 2 * * 1`；2026-08-24 cron 优化由每天改每周一，与夜间迭代周一复盘合并）
- 用户说"你又忘了""规则没执行""这个规则不是说过吗"
- 夜间自主学习/迭代中做机制自检时

## 审计报告 AAR 结构（2026-08-22 借鉴 SkillHub「复盘-AAR 事后回顾法」）

审计报告（迭代日报/rules-audit.md）除检出清单外，按 AAR 四步组织：
1. **预期 vs 实际**：规则本应达到的效果 vs 实际执行情况（变形点即"实际≠预期"）
2. **差异根因**：为什么变形（载体未同步/协议未固化/执行时遗忘）
3. **经验**：本次学到什么（可复用模式）
4. **行动**：具体修复动作（写入哪个载体、下次如何避免）

周维度复盘模板（夜间迭代周一使用）：
```
# 周复盘
## 本周完成（规则/机制层面）
## 未完成及原因
## 最大卡点（哪条规则执行最弱）
## 下周 Top3（要强化的规则）
## 一个要改的习惯
```

## 规则四问检查（2026-08-21 用户确认）

审计时对近7天新规则/新方案跑四问：①效果会好吗 ②若失败死因是什么 ③是否主流方案 ④网络有无优秀案例。非最优或过时规则→标记"建议优化"写入审计报告，不擅自修改，等用户拍板。

## 高频遗忘规则清单（2026-08-21 加固，每次审计必查）

以下 4 条是用户多次纠正、且直接影响输出效果的规则——每次审计先查这 4 条是否在 memory/skill 且执行正常：

1. **输出纪律**：只给结果，禁过程/元叙述/引导语（memory 核心铁律⑩ + user-protocol 自检第11条）
2. **闲时操作节约**：批量/耗时/重操作放低价时段（12-14/18-次日9）（memory 待办队列 + user-task-queue-protocol"闲时操作节约"章节）
3. **编号指令回溯**：收到编号指令先回溯原清单复述确认（user-protocol 编号指令铁律）
4. **串行禁并发**：批量/多账号/delegate 严格串行逐个（memory 核心铁律⑦）

检查方法：session_search 近期对话，若出现违反以上任一条的回复（元叙述/高峰跑批量/编号乱猜/并发执行）→ 立即强化固化并记录到审计日志。执行正常 → 标注"正常"。

## 执行步骤

1. **采集**：session_search 搜索近7天对话，关键词：
   - "以后""记住""规则""要求""必须""不要""禁止""改成""统一""每次""一律"
   - 提取用户设定的规则/偏好/限制（排除一次性任务指令）
   - "skill""技能"相关：检查回复中 skill 是否用中文名（禁英文 ID，用户 2026-08-20 强调，SOUL.md 硬约束）
2. **对照**：读取 MEMORY.md / USER.md（memory tool）+ skills_list 全量
   - 每条规则检查是否在 memory 有"一句话触发条件"
   - 复杂规则检查是否有对应 skill 文件
3. **补缺**：
   - 缺失 → 写入 memory（一句话，精简）或 skill（详细流程）
   - memory 满 → 先压缩/合并旧条目再写（用 operations 批量）
4. **报告**：输出审计结果：
   - 新检出规则 N 条
   - 已补齐 M 条
   - 已在册无需处理 K 条
   - 写审计日志到 /home/user/learnings/迭代日报/ 当日文件
5. **验证**：对补齐的关键规则，下一轮对话中主动提及一次以确认生效

## 边界

- 只补"规则/偏好/限制"类，不补一次性任务进度
- 不确定是否算规则 → 先写入 memory（宁多勿漏，容量允许时）
- memory 容量紧张时：优先保留 高频触发/隐私红线/沟通偏好，任务进度类移出
- 禁止在审计中修改 cron 配置（那是运维范畴，另走队列）

## 关键文件

- memory: MEMORY.md / USER.md（memory tool 管理）
- 队列: /home/user/queue/pending_tasks.md
- 审计日志: /home/user/learnings/迭代日报/
